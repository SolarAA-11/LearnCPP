#include <iostream>

#include "lc_helper.h"

using namespace lc;

class Solution {
public:
    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
        ListNode* pResultNodeList{0};
        while(l1 != nullptr && l2 != nullptr) {

        }
    }
};

int main() {
    return 0;
}